import { constantes } from "../classes/constantes"

//Jaime Ortega
export const obtenerMapaBase = async () => {
    const rutaMapa = constantes.urlApi + constantes.mapa

    try {
        
    } catch (e) {
        
    }
}